---
phase: 09-skills-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/services/executors/skill_executor.py
  - backend/app/api/v1/settings/skills.py
  - backend/app/tests/test_skills.py
  - frontend/src/components/settings/skills-section.tsx
  - frontend/src/__tests__/skills.test.tsx
autonomous: true
requirements: [SKILL-01, SKILL-02, SKILL-03]

must_haves:
  truths:
    - "User can view available skills (web_search, code_execution) in Settings > Skills tab"
    - "User can toggle each skill on or off and the change persists"
    - "Enabled skills are registered in ExecutorRegistry at each chat turn"
    - "SkillExecutor emits skill_start and skill_end trace events visible in TracePanel"
    - "Skill execution metadata persists in the database as part of message trace_data"
  artifacts:
    - path: "backend/app/models/skill.py"
      provides: "Skill SQLModel with name, description, is_enabled, config, created_at"
      contains: "class Skill"
    - path: "backend/app/services/executors/skill_executor.py"
      provides: "SkillExecutor implementing BaseExecutor protocol with trace emission"
      exports: ["SkillExecutor", "DEFAULT_SKILL_HANDLERS"]
    - path: "backend/app/api/v1/settings/skills.py"
      provides: "GET / (list) and PATCH /{id}/toggle endpoints"
      exports: ["router"]
    - path: "backend/alembic/versions/0008_add_skill_table.py"
      provides: "Alembic migration for skill table"
      contains: "create_table"
    - path: "frontend/src/lib/skills-api.ts"
      provides: "listSkills and toggleSkill API client functions"
      exports: ["listSkills", "toggleSkill", "SkillRead"]
    - path: "frontend/src/components/settings/skills-section.tsx"
      provides: "SkillsSection component with toggle switches"
      exports: ["SkillsSection"]
    - path: "backend/app/tests/test_skills.py"
      provides: "Backend tests for skills API and SkillExecutor"
      min_lines: 100
    - path: "frontend/src/__tests__/skills.test.tsx"
      provides: "Frontend tests for SkillsSection component"
      min_lines: 100
  key_links:
    - from: "frontend/src/components/settings/skills-section.tsx"
      to: "/api/v1/settings/skills/"
      via: "skills-api.ts listSkills/toggleSkill"
      pattern: "apiFetch.*settings/skills"
    - from: "backend/app/api/v1/chat.py"
      to: "backend/app/services/executors/skill_executor.py"
      via: "ExecutorRegistry.register for enabled skills"
      pattern: "registry\\.register\\(skill\\.name"
    - from: "backend/app/services/executors/skill_executor.py"
      to: "backend/app/services/trace_emitter.py"
      via: "TraceEmitter emit_tool_start/emit_tool_end"
      pattern: "tracer\\.emit_tool"
---

<objective>
Validate that the Skills Integration feature is fully implemented and all tests pass.

Purpose: Phase 9 was implemented across prior work (quick tasks and adjacent phases). This plan validates completeness against SKILL-01, SKILL-02, SKILL-03 requirements and ensures all artifacts are present and tests pass.

Output: Verification that all skills feature code exists, tests pass, and the feature works end-to-end.
</objective>

<execution_context>
@/Users/przbadu/.claude/get-shit-done/workflows/execute-plan.md
@/Users/przbadu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-skills-integration/9-CONTEXT.md

<interfaces>
<!-- Existing code — already implemented. Executor reads to verify, not build. -->

From backend/app/services/executors/base.py:
```python
class BaseExecutor(Protocol):
    async def execute(self, name: str, input: dict[str, Any]) -> ExecutorResult: ...

@dataclasses.dataclass
class ExecutorResult:
    output: Any
    error: str | None = None
```

From backend/app/services/executors/registry.py:
```python
class ExecutorRegistry:
    def register(self, tool_name: str, executor: BaseExecutor) -> None: ...
    async def dispatch(self, tool_name: str, input: dict[str, Any]) -> ExecutorResult: ...
    def available_tools(self) -> list[str]: ...
```

From backend/app/services/executors/skill_executor.py:
```python
class SkillExecutor:
    async def execute(self, name: str, input: dict[str, Any]) -> ExecutorResult: ...
```

From backend/app/api/v1/settings/skills.py:
```python
GET  /api/v1/settings/skills/           -> list[SkillRead]
PATCH /api/v1/settings/skills/{id}/toggle -> SkillRead
```

From frontend/src/lib/skills-api.ts:
```typescript
export interface SkillRead { id: number; name: string; description: string; is_enabled: boolean; config: string | null; created_at: string; }
export async function listSkills(token: string): Promise<SkillRead[]>;
export async function toggleSkill(token: string, id: number): Promise<SkillRead>;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify backend skills implementation and tests</name>
  <files>backend/app/tests/test_skills.py</files>
  <read_first>
    backend/app/models/skill.py
    backend/app/services/executors/skill_executor.py
    backend/app/services/executors/base.py
    backend/app/api/v1/settings/skills.py
    backend/app/api/v1/chat.py
    backend/app/main.py
    backend/app/tests/test_skills.py
  </read_first>
  <action>
    Run the backend skills test suite to confirm all 9 tests pass. Verify the following implementation artifacts exist and are correct:

    1. Skill model in backend/app/models/skill.py has fields: id, name, description, is_enabled, config, created_at
    2. Alembic migration 0008_add_skill_table.py creates the skill table with unique index on name
    3. SkillExecutor in backend/app/services/executors/skill_executor.py implements BaseExecutor protocol, emits trace events via TraceEmitter (emit_tool_start, emit_tool_end), has DEFAULT_SKILL_HANDLERS for web_search and code_execution
    4. API endpoints in backend/app/api/v1/settings/skills.py: GET / returns list[SkillRead], PATCH /{id}/toggle flips is_enabled
    5. Skills router is mounted at /api/v1/settings/skills in router.py
    6. chat.py registers enabled skills in ExecutorRegistry at each chat turn
    7. main.py seeds DEFAULT_SKILLS (web_search, code_execution) on startup via seed_default_skills()
    8. All 9 backend tests pass: list skills, toggle, auth required, not found, executor handlers, unknown skill, custom handler, trace events, handler error

    Run: cd backend && .venv/bin/python -m pytest app/tests/test_skills.py -v

    If any test fails, fix the issue. If all pass, this task is complete.
  </action>
  <verify>
    <automated>cd /Users/przbadu/dev/claude-clone/backend && .venv/bin/python -m pytest app/tests/test_skills.py -v --tb=short 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "class Skill" backend/app/models/skill.py
    - grep -q "class SkillExecutor" backend/app/services/executors/skill_executor.py
    - grep -q "emit_tool_start" backend/app/services/executors/skill_executor.py
    - grep -q "emit_tool_end" backend/app/services/executors/skill_executor.py
    - grep -q "DEFAULT_SKILL_HANDLERS" backend/app/services/executors/skill_executor.py
    - grep -q "registry.register(skill.name" backend/app/api/v1/chat.py
    - grep -q "seed_default_skills" backend/app/main.py
    - Backend test output contains "9 passed"
  </acceptance_criteria>
  <done>All 9 backend skills tests pass. Skill model, SkillExecutor with trace emission, API endpoints, registry wiring, and seed data are all verified present and correct.</done>
</task>

<task type="auto">
  <name>Task 2: Verify frontend skills implementation and tests</name>
  <files>frontend/src/__tests__/skills.test.tsx</files>
  <read_first>
    frontend/src/components/settings/skills-section.tsx
    frontend/src/lib/skills-api.ts
    frontend/src/app/(protected)/settings/page.tsx
    frontend/src/__tests__/skills.test.tsx
  </read_first>
  <action>
    Run the frontend skills test suite to confirm all 5 tests pass. Verify the following implementation artifacts exist and are correct:

    1. skills-api.ts exports SkillRead type, listSkills(), and toggleSkill() functions using apiFetch
    2. SkillsSection component renders skill list with Switch toggles, loading state, error state, and empty state
    3. Settings page includes Skills tab with SkillsSection component
    4. All 5 frontend tests pass: empty state, skill list rendering, toggle switches, toggle API call, error state

    Run: cd frontend && npx vitest run src/__tests__/skills.test.tsx --reporter=verbose

    If any test fails, fix the issue. If all pass, this task is complete.
  </action>
  <verify>
    <automated>cd /Users/przbadu/dev/claude-clone/frontend && npx vitest run src/__tests__/skills.test.tsx --reporter=verbose 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "SkillsSection" frontend/src/components/settings/skills-section.tsx
    - grep -q "listSkills" frontend/src/lib/skills-api.ts
    - grep -q "toggleSkill" frontend/src/lib/skills-api.ts
    - grep -q "skills" frontend/src/app/(protected)/settings/page.tsx
    - Frontend test output contains "5 passed"
  </acceptance_criteria>
  <done>All 5 frontend skills tests pass. SkillsSection component, skills-api client, and Settings tab wiring are all verified present and correct.</done>
</task>

</tasks>

<verification>
All three SKILL requirements validated:

- SKILL-01: User can view and enable/disable skills in Settings
  - Backend: GET /api/v1/settings/skills/ returns seeded skills, PATCH /{id}/toggle flips is_enabled
  - Frontend: SkillsSection renders skill list with Switch toggles, Settings page has Skills tab

- SKILL-02: Skill triggers and outputs appear in message execution trace
  - SkillExecutor.execute() calls tracer.emit_tool_start() and tracer.emit_tool_end()
  - chat.py registers enabled skills in ExecutorRegistry per chat turn
  - TracePanel (from Phase 6) renders tool_call events which includes skill events

- SKILL-03: Skill execution metadata persists in the database
  - TraceEmitter accumulates events, stored as trace_data JSON blob on Message (Phase 6 mechanism)
  - Skill events (type=tool_call) are included in the same trace_data persistence path
</verification>

<success_criteria>
- All 9 backend skills tests pass (pytest)
- All 5 frontend skills tests pass (vitest)
- Skill model, SkillExecutor, API endpoints, frontend components, seed data all exist
- SkillExecutor emits trace events (emit_tool_start, emit_tool_end)
- Enabled skills are registered in ExecutorRegistry in chat.py
</success_criteria>

<output>
After completion, create `.planning/phases/09-skills-integration/09-01-SUMMARY.md`
</output>
